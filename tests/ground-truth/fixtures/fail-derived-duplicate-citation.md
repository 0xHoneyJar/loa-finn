---
title: "DERIVED Duplicate Citation Fail Fixture"
---

# Test: DERIVED with Duplicate Citations Should Fail

<!-- provenance: DERIVED -->
The WAL rotation threshold is configured at 10MB (`src/persistence/wal.ts:42`) and verified in the rotation logic (`src/persistence/wal.ts:42`).
