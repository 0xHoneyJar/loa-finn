# {Project Name} — API Reference

> Generated by Ground Truth v1.0.0 | {generated_at}
> HEAD: {head_sha} | Grounded in: /ride reality + features.yaml

<!-- TEMPLATE-META
type: api-reference
size_limit: 800
provenance_min: 80
required_sections: 6
variables: none
-->

---

## Authentication Overview

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: symbol=X -->
{JWT dual-auth vs bearer token authentication. Cite middleware files.}

---

## Endpoint Table

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: symbol=X, symbol=Y -->
{Complete table of all HTTP endpoints.}

| Method | Path | Auth | Classification | Handler |
|--------|------|------|----------------|---------|
| `{METHOD}` | `{/path}` | {auth_type} | {public/internal/admin} | `{file}:{line}` |

---

## Per-Endpoint Details

### Public Endpoints

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: symbol=X -->

#### `{METHOD} {/path}`

{Endpoint description.}

**Request**:
```json
{request_body_schema}
```

**Response** (`{status_code}`):
```json
{response_body_schema}
```

**Rate Limiting**: {rate_limit_info or "None"}

### Internal Endpoints

<!-- provenance: CODE-FACTUAL -->
{Internal endpoints with same format as above.}

### Admin Endpoints

<!-- provenance: CODE-FACTUAL -->
{Admin endpoints with same format as above.}

---

## WebSocket Contracts

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: symbol=X -->
{If applicable: message types, schemas, auth handshake, error frames.}

### Message Types

| Type | Direction | Schema | Description |
|------|-----------|--------|-------------|
| `{type}` | {client→server/server→client} | {schema_ref} | {description} |

### Auth Handshake

{WebSocket authentication flow.}

### Error Frames

{Error frame format and codes.}

---

## Error Response Format

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: symbol=X -->
{Standard error response format used across all endpoints.}

```json
{
  "error": "{error_code}",
  "message": "{human_readable_message}",
  "details": {}
}
```

<!-- ground-truth-meta: head_sha={sha} generated_at={timestamp} features_sha={sha} limitations_sha={sha} ride_sha={sha} -->
