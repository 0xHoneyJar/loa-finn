# Three-Way Drift Report — EXP-004 Grounding Pass

> Generated by `/ride` 2026-06-12 (EXP-004 graduation-gate lens). Refreshes the experiment-program +
> score/cost/substrate surface; the 2026-06-08 infrastructure findings are carried forward in §6.
>
> **Three axes**: (1) CODE truth (`src/`), (2) grimoire docs (the EXP program + economics +
> epistemology + the finn-economy-os PRD/SDD), (3) the just-settled **EXP-003 GO** (vertical-first /
> NO-GO horizontal, 2026-06-12) + the **#269 rescope lesson** (a deterministic formula is not the
> product; the substrate must be validated first).
>
> Scope note: this pass does **not** regenerate `prd-finn-economy-os.md` / `sdd-finn-economy-os.md`
> (the product SoT, 2026-06-08); stale claims there are flagged for a targeted amendment, not overwritten.

## Summary

| Metric | Value |
|--------|-------|
| Experiment-program drift score | **4.0 / 10** (Moderate — docs are honest about scope, but two headline theses are now stale vs their own evidence) |
| Aligned (healthy) | Cost meter ↔ three-ledger economics · substrate runtime ↔ sandbox claim · observatory spine ↔ EXP-001/003 data · score core ↔ Sprint-1 plan |
| Stale | `arch-finn-cost-of-play.md` H1 "infra-dominated" thesis (own readout falsifies it) · PRD horizontal/"credit-bureau" framing vs EXP-003 GO-vertical |
| Status drift (EXP-004 critical) | `src/score` reads as "sybil layer present" but is fixture-fed, internal-only, adapters `NotImplementedError`, no validation harness |
| Over-claims flagged | 5 (see §3) |
| Carried-forward CRITICAL | `package.json` license `MIT` vs actual AGPL-3.0 — still open (§6) |

---

## 1. The central finding (EXP-004 graduation gate)

**The deterministic formula is built and unit-tested. The substrate around it is not validated.**
This is the #269 lesson made literal inside loa-finn's own repo.

`src/score/index.ts:1-13` exposes the FR-6 port + adapters + pure analysis core "only — no ingestion,
persistence, report, or publication (Sprints 2–4)." The core (`leaderboard.ts`, `features.ts`,
`cluster.ts`, `screen.ts`) is pure, deterministic, and covered by unit tests
(`src/score/core/__tests__/*`, `src/score/__tests__/import-boundary.test.ts`). Commits `104bf95d`
(S1 core) and `cb0a5738` (S1 GPT-5 review fixes) on branch `feature/score-phase1`.

But the layer that makes it a *product* is absent:

| EXP-004 scope element | Code reality today | Citation | Gap |
|---|---|---|---|
| Deterministic screen (the "formula") | **BUILT** — pure + unit-tested | `src/score/core/screen.ts`, `features.ts`, `cluster.ts`, `leaderboard.ts` | none — formula done (Sprint 1) |
| Real sybil layer = screen over **real** data | **NOT BUILT** — runs on fixtures only | `src/score/edge/adapters.ts:22,36` (both `fetchEpochGraph` throw `NotImplementedError`) | **Sprint 2**: real read-only Base/ACP ingestion (`ALCHEMY_API_KEY`, `rpc-pool` `getLogs`/`getBlock`) |
| Labeled ground-truth validation harness | **NOT BUILT** — bar is a placeholder | `src/score/core/screen.ts:37` (`precisionBar` "carried here for the contract") | **Sprint 3 (FR-2a)**: hand-labeled set + precision/recall + downgrade to INSUFFICIENT-EVIDENCE if precision < bar |
| Publication surface (under the gate) | **NOT BUILT** — screen is internal | `src/score/core/screen.ts:5` ("consumed by the Sprint-3 validation harness … NOT posted") | **Sprint 4**: publication-hold + legal review |

**Grounding for EXP-004 scope**: EXP-004 ≡ **Sprint 2 + Sprint 3** of `sprint-finn-score.md`
(`:104-189`). Both are pre-registered with acceptance criteria but unbuilt. The graduation bar every
score must clear — *real ingestion + precision-gated validation before any named label is published* —
is FR-2a (`sdd-finn-economy-os.md §7.2`), embodied today only as the unused `precisionBar` field.

**Why this is "status drift," not a doc lie**: the docs (PRD §5.2, sprint plan, `index.ts` header)
are honest that Sprints 2–4 are future. The risk is at the *barrel*: `export * from "./core/screen.js"`
makes `screenAnomaly`/`naive_farm`/`subsidy_capture` importable, so a reader can mistake "the screen
exists" for "the sybil layer ships." The screen detects on a `TxGraph` that nothing currently fills.

---

## 2. Drift by surface

### D1 — Score machinery — STATUS DRIFT (CRITICAL for EXP-004) — see §1.

### D2 — `arch-finn-cost-of-play.md` H1 thesis — STALE (HIGH)
- **Doc claim** (`arch-finn-cost-of-play.md:16-17`): "Cost is infra-dominated, not inference-dominated
  → reduce the agentic inference surface." The whole architecture leads with this thesis.
- **Code/data truth**: the experiment's own verdict artifact **FALSIFIES** it. `readout.json` (EXP-001,
  4,378 atoms): **H1 FALSIFIED** — per-Class-B inference share **93.7%**, blended 92.8%
  (inference-*dominated*). **H2 FALSIFIED** — unit cost flat across load (L1 $0.1437 → L3 $0.1479,
  R²=0.018, no amortization). **H3 HELD** — cheval spawn 74 ms, orchestration share 0.0001.
- **EXP-003/#269 lens**: this is the realness-filter discipline working as designed — a pre-registered
  bar caught an over-confident thesis. But the spec still *reads* as if infra-dominance holds. **Fix**:
  fold the falsification into the doc's lede; the operating thesis is now "inference-dominated; near-zero
  marginal cost is the survival condition" (already the post-handoff correction at `:52-55`).

### D3 — PRD "credit bureau" / horizontal framing — STALE vs EXP-003 GO (MEDIUM)
- **Doc** (`prd-finn-economy-os.md:68-70`): "Score as the agent-economy credit bureau" (guarded as
  Strategic Hypothesis). Distribution/Commercial GO gates (`:94-109`) are horizontal-flavored
  (quote-tweets, inbounds, mindshare).
- **EXP-003 truth** (settled 2026-06-12, `exp3-c2-settle.md:19-23,39,46-52`): **GO on VERTICAL ·
  NO-GO on HORIZONTAL**. Verification demand is real but in-house/vertical (Harvey ~$190M CEO-sourced,
  Sierra, Decagon); horizontal agent-to-agent is pre-revenue. The sharpened wedge: Score's **no-LLM
  determinism is now a technical advantage** — it sidesteps the LLM floating-point consensus problem
  that breaks ARBITER-style re-execution quorums (`exp3-c2-settle.md:27-29,46-52`).
- **Drift**: the PRD (2026-06-08) predates this settlement; the vertical-first + no-LLM-as-moat reframe
  is not folded in. **Fix**: amend PRD §1.2 / §7 (moat) with the EXP-003 GO and the determinism-as-edge
  argument. This *strengthens* the existing FR-6 substrate-agnostic moat down-payment.

### D4 — Cost meter ↔ three-ledger economics — ALIGNED (healthy)
`src/cost/cost-atom.ts:30-71` implements exactly the `experiment-economics.md:29-33` claim: three
separate ledgers (`InferenceLedger`/`InfraLedger`/`OrchestrationLedger`), never pooled until close;
integer micro-USD only (no float leakage); sum invariant (`closeAtom`, `:346-394`); append-only writer.
The economics doctrine and the code are the same object. No drift.

### D5 — Substrate runtime ↔ "Finn spawns its own sandboxes" — INFERRED (aspirational, no over-claim)
`src/substrate/` is real: worker-thread sandbox (`worker-runtime.ts`), JWT-licensed constructs
(`jwt-validator.ts`), capability-bounded `ModelRunner`/`EventWriter` (`runtime.ts:77-124`), fail-closed
invoke timeout (`sandbox-bridge.ts`). The `experiment-economics.md:18-19` framing — "Finn is the
experiment; the construct that composes constructs and spawns its own sandboxes" — is explicitly tagged
vision (deferred to vision-010). The executor exists; the self-experiment framing is intent. No drift.

### D6 — Observatory spine ↔ EXP-001/003 data — ALIGNED
`observatory/scripts/refresh-data.mjs` reads cost-atom JSONL + driver phases + `readout.json` →
`observatory/src/lib/data.generated.json` (8.7 KB, regenerated 2026-06-12). The experiment-program
visualization spine is wired to the same atoms the cost meter writes. No drift.

### D7 — EXP-002 forensic reclassification — FINDING (load-bearing, primary-source pending)
`experiment-economics.md:121-127` records the settled findings: `0xa6c9ba86` = PRIZE_DISTRIBUTOR,
$320.9M reclassified OUT of "agent commerce"; genuine A2A ≈ $0; V2 PaymentManager real settled commerce
= **$0.30 / 24 jobs**. These are the premise Score exists to surface (the leaderboard is ~95% wash).
Not a code-drift; flagged in the briefs as flash/forensic, awaiting primary-source per EXP-003 rigor.

---

## 3. Over-claims flagged (operator ask: "where do the docs over-claim vs the code")

| # | Over-claim | Reality | Severity |
|---|-----------|---------|----------|
| 1 | `arch-finn-cost-of-play.md:16` leads with "cost is infra-dominated" | `readout.json` H1 FALSIFIED (93.7% inference) | **HIGH** (stale) |
| 2 | `src/score` barrel exports the screen as if a shippable sybil layer | fixture-fed, adapters `NotImplementedError`, internal-only, no validation harness | **CRITICAL** (EXP-004 scope) |
| 3 | PRD frames Score as horizontal "credit bureau" | EXP-003 GO is **vertical**; horizontal is NO-GO/pre-revenue | **MEDIUM** (stale) |
| 4 | PRD §5.1 calls organs "VERIFIED" (by `/ride --enriched`) | `/ride` is reconnaissance, not audit — proves *exist*, not *production-grade* | **LOW** |
| 5 | `epistemology-deterministic-layers.md:33,88` states "standing operator doctrine" / "models never settle verdicts" as enforced | honored by convention in EXP-001; **no code gate** enforces "non-LLM verdicts only" | **LOW** |

---

## 4. Drift categories (code vs docs)

| Category | Count | Items |
|----------|-------|-------|
| Aligned | 4 | cost meter, substrate executor, observatory spine, score-core ↔ Sprint-1 plan |
| Stale | 2 | cost-of-play H1 thesis, PRD horizontal framing |
| Status drift | 1 | score "layer present" vs fixture-fed/unvalidated (EXP-004 gap) |
| Hallucinated | 0 | — |
| Ghost (doc claims, no code) | 1 | FR-2a validation harness (described as the gate; only `precisionBar` placeholder in code) |
| Shadow (code, no doc) | 0 | score core is well-documented in the sprint plan |

---

## 5. Recommended actions (no code written by this ride)

1. **EXP-004 = Sprint 2 + Sprint 3** of `sprint-finn-score.md`. Build real ingestion
   (`AgdpAcpAdapter.fetchEpochGraph`) then the FR-2a precision/recall harness *before* any named-label
   publication. The graduation bar is the same for every score in the repo.
2. **Amend `arch-finn-cost-of-play.md`** lede: replace the infra-dominated thesis with the falsified
   verdict + near-zero-marginal-cost survival framing (the `:52-55` correction is already written).
3. **Amend `prd-finn-economy-os.md` §1.2/§7**: fold the EXP-003 GO-vertical decision and the
   no-LLM-determinism-as-moat argument; downgrade the horizontal "credit bureau" framing to a later phase.
4. **Soften "VERIFIED" → "code-present (recon)"** in PRD §5.1, reserving "verified" for audited organs.

---

## 6. Carried forward from 2026-06-08 (infrastructure surface — still open)

These findings are outside the experiment-program scope of this pass but remain unresolved:

- **CRITICAL — License mismatch**: `package.json:6` declares `"license": "MIT"`, actual is AGPL-3.0
  (`LICENSE.md`, `README.md`). Legally misleading. Fix to `"AGPL-3.0-or-later"`.
- **STALE — Reality counts** (now updated this pass): 363 non-test `.ts`, ~83.6K LOC, 27 src modules,
  381 test files, 142 env vars, 87 route mounts, 230 git tags.
- **SHADOW — Undocumented modules** still without `docs/modules/` entries: `nft`, `x402`, `credits`,
  `billing`, `substrate`, `marketplace`, `events`, `score`, `cost`.
- **"Minimal runtime"** in `package.json:5` description no longer accurate at ~83.6K LOC / 27 modules.
