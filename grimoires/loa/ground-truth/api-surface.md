---
title: loa-finn — API Surface
version: 1.0.0
last_updated: 2026-02-28
commit: b4a3075
---

# loa-finn — API Surface

> Generated by Ground Truth v1.0.0
> Grounded in: codebase analysis at commit b4a3075

## Overview

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/server.ts -->
loa-finn exposes 25+ HTTP endpoints, 1 WebSocket endpoint, and 4 scheduled jobs. The gateway is built on Hono v4 (`src/gateway/server.ts`) with route groups for sessions, tenants, oracle, identity, discovery, admin, x402 payments, and metrics.

## Health & Observability

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/server.ts:94-207 -->

### GET /healthz
- **Handler**: `src/gateway/server.ts:106`
- **Auth**: None
- **Response**: `{ status: "ok", uptime: number }`
- **Purpose**: ALB liveness probe — always 200 if process alive

### GET /health/deps
- **Handler**: `src/gateway/server.ts:112`
- **Auth**: None
- **Response**: `{ status: "ready|not_ready", checks: { redis, dynamodb } }`
- **Status**: 200 (healthy) or 503 (dependency unreachable)
- **Purpose**: Readiness probe for critical dependencies

### GET /health
- **Handler**: `src/gateway/server.ts:145`
- **Auth**: None
- **Response**: Full system snapshot — billing DLQ, protocol version, Goodhart routing state, audit chain, relayer balance, worker pool, provider health
- **Purpose**: Operator health dashboard

### GET /metrics
- **Handler**: `src/gateway/server.ts:528`
- **Auth**: Bearer token (`FINN_METRICS_BEARER_TOKEN`) if configured
- **Response**: Prometheus-format text
- **Purpose**: Graduation metrics for monitoring

## Session Management

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/server.ts:345-425 -->

### POST /api/sessions
- **Handler**: `src/gateway/server.ts:345`
- **Auth**: Bearer token (authMiddleware)
- **Response** (201): `{ sessionId, created, wsUrl }`

### GET /api/sessions
- **Handler**: `src/gateway/server.ts:409`
- **Auth**: Bearer token
- **Response**: `{ sessions: Array<{ id, state, ... }> }`

### GET /api/sessions/:id
- **Handler**: `src/gateway/server.ts:414`
- **Auth**: Bearer token
- **Response**: `{ id, state, isStreaming, messageCount }`
- **Status**: 404 if not found

### POST /api/sessions/:id/message
- **Handler**: `src/gateway/server.ts:363`
- **Auth**: Bearer token
- **Response**: `{ response, toolCalls }`

## Hounfour Tenant API

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/invoke.ts:34, file=src/gateway/routes/usage.ts:28 -->

### POST /api/v1/invoke
- **Handler**: `src/gateway/routes/invoke.ts:34`
- **Auth**: JWT (hounfourAuth middleware → TenantContext)
- **Request**: `{ agent: string, prompt: string }`
- **Response**: `{ response, model, usage: { prompt_tokens, completion_tokens, total_tokens }, cost_micro, trace_id, knowledge }`
- **Error codes**: 400 (invalid), 401 (unauthed), 402 (budget exceeded), 413 (context overflow), 422 (model unavailable), 429 (rate limited), 502 (provider down), 503 (circuit breaker)

#### Error Response Schemas

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/invoke.ts:12-25, file=src/gateway/routes/invoke.ts:74-94, file=src/hounfour/errors.ts:4-33 -->

All error responses use the canonical shape: `{ error: string, code: string }`

| Status | Code | `error` message | Trigger |
|--------|------|-----------------|---------|
| 400 | `INVALID_REQUEST` | `"prompt is required..."` or `"agent is required..."` or `"Invalid JSON body"` | Missing/empty fields or malformed JSON (`invoke.ts:42-57`) |
| 401 | `TENANT_CONTEXT_MISSING` | `"Unauthorized"` | JWT middleware did not set TenantContext (`invoke.ts:37-39`) |
| 402 | `BUDGET_EXCEEDED` | `"Budget exceeded"` | BudgetEnforcer denies request (`invoke.ts:14,78`) |
| 403 | `KNOWLEDGE_INJECTION` | `"Knowledge source rejected for security"` | Knowledge enricher security filter (`invoke.ts:85`) |
| 413 | `CONTEXT_OVERFLOW` | `"Context window exceeded"` | Prompt + knowledge exceeds model context window (`invoke.ts:86`) |
| 422 | `ORACLE_MODEL_UNAVAILABLE` | `"Model context window insufficient for knowledge enrichment"` | No model can fit the knowledge payload (`invoke.ts:83`) |
| 429 | `RATE_LIMITED` | `"Rate limit exceeded"` | Per-tenant RPM/TPM limit hit (`invoke.ts:82`) |
| 502 | `PROVIDER_UNAVAILABLE` | `"Upstream provider unavailable"` | All providers failed (`invoke.ts:81`) |
| 503 | `BUDGET_CIRCUIT_OPEN` | `"Service temporarily unavailable"` | Circuit breaker OPEN state (`invoke.ts:16,80`) |
| 500 | `INTERNAL_ERROR` | `"Internal error"` | Unexpected non-HounfourError (`invoke.ts:92-93`) |

**Note**: No `Retry-After` header on 429 — client must implement its own backoff. Circuit breaker cooldown is 30s (`src/billing/circuit-breaker.ts:18`) but not exposed to clients.

### GET /api/v1/usage
- **Handler**: `src/gateway/routes/usage.ts:28`
- **Auth**: JWT (tenant-isolated)
- **Query**: `days` (1-90, default 7)
- **Response**: `{ tenant_id, period, total_cost_micro, total_requests, by_model, settlement_status }`

## Oracle Product

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/oracle.ts:40 -->

### POST /api/v1/oracle
- **Handler**: `src/gateway/routes/oracle.ts:40`
- **Auth**: Redis-backed API key (oracleAuthMiddleware)
- **Request**: `{ question: string (max 10K chars), context?: string (max 5K chars) }`
- **Response**: `{ answer, sources: [{ id, tags, tokens_used }], metadata: { knowledge_mode, retrieval_ms, model } }`
- **Error codes**: 400, 402 (daily ceiling), 413, 422, 429, 503
- **Middleware stack**: CORS → billing conservation guard → auth → rate limiting → concurrency limiting → corpus version header

## Identity & NFT

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/identity.ts:51-79, file=src/gateway/routes/agent-public-api.ts:45 -->

### GET /api/identity/wallet/:wallet/nfts
- **Handler**: `src/gateway/routes/identity.ts:51`
- **Auth**: None
- **Response**: `{ nfts: [{ collection, tokenId, title }], total }`

### GET /api/identity/wallet/:wallet/nft
- **Handler**: `src/gateway/routes/identity.ts:79`
- **Auth**: None
- **Deprecation**: Sunset 2026-09-01 — use `/nfts` instead

### GET /api/v1/public
- **Handler**: `src/gateway/routes/agent-public-api.ts:45`
- **Auth**: None
- **Query**: `tokenId` (required)
- **Response**: `{ display_name, archetype, reputation_state, conversation_count }`
- **Cache**: Redis (5-minute TTL)

## Conversations

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/conversations.ts:45-105 -->

### POST /api/v1/conversations
- **Handler**: `src/gateway/routes/conversations.ts:45`
- **Auth**: SIWE JWT (requireSiweSession)
- **Request**: `{ nft_id: string }`

### GET /api/v1/conversations
- **Handler**: `src/gateway/routes/conversations.ts:74`
- **Auth**: SIWE JWT
- **Query**: `nft_id` (required), `cursor`, `limit`

### GET /api/v1/conversations/:id/messages
- **Handler**: `src/gateway/routes/conversations.ts:105`
- **Auth**: SIWE JWT
- **Query**: `cursor`, `limit`

## API Key Management

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/keys.ts:36-73 -->

### POST /api/v1/keys
- **Handler**: `src/gateway/routes/keys.ts:36`
- **Auth**: SIWE JWT
- **Response** (201): `{ key_id, plaintext_key, message: "Store this key securely..." }`

### DELETE /api/v1/keys/:key_id
- **Handler**: `src/gateway/routes/keys.ts:59`
- **Auth**: SIWE JWT
- **Response**: `{ key_id, revoked: true }`

### GET /api/v1/keys/:key_id/balance
- **Handler**: `src/gateway/routes/keys.ts:73`
- **Auth**: SIWE JWT
- **Response**: `{ key_id, balance_micro }`

## Admin

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/admin.ts:108-121 -->

### GET /api/v1/admin/mode
- **Handler**: `src/gateway/routes/admin.ts:108`
- **Auth**: JWKS JWT (ES256, operator|admin role)
- **Response**: `{ mode: "enabled|disabled|shadow", timestamp }`

### POST /api/v1/admin/mode
- **Handler**: `src/gateway/routes/admin.ts:121`
- **Auth**: JWKS JWT (operator|admin role)
- **Request**: `{ mode: "enabled|disabled|shadow" }`
- **Rate limit**: 5 mode changes per subject per hour
- **Semantics**: Audit-first — writes audit intent before Redis set

## x402 Payment

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/x402-routes.ts -->

### POST /api/v1/x402/invoke
- **Auth**: None (payment-based via `X-Payment` header)
- **Request**: `{ model?, max_tokens?, prompt }`
- **402 Response**: `{ error: "Payment required", code: "PAYMENT_REQUIRED", quote }`
- **200 Response**: `{ response, model, usage }`
- **Feature flags**: `x402` (enable), `x402:public` (allowlist gating)
- **Rate limit**: 100 req/hour per wallet

### POST /api/v1/pay/chat
- **Alias**: Same handler as `/api/v1/x402/invoke`

## Discovery

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/routes/discovery.ts:36-58 -->

| Endpoint | Format | Purpose |
|----------|--------|---------|
| `GET /openapi.json` | JSON | OpenAPI 3.1 specification |
| `GET /llms.txt` | Plain text | Agent capability manifest |
| `GET /agents.md` | Markdown | Human-readable agent directory |
| `GET /.well-known/jwks.json` | JSON | JWKS public key set for S2S JWT verification |

## WebSocket

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/gateway/ws.ts:45, file=src/index.ts:742 -->

### WS /ws/:sessionId
- **Handler**: `src/gateway/ws.ts:45`
- **Auth**: Query `?token=...` (optional)
- **Limits**: 5 connections/IP, 1MB payload, 5min idle timeout

| Direction | Types |
|-----------|-------|
| Client → Server | `prompt`, `steer`, `abort`, `ping` |
| Server → Client | `text_delta`, `tool_start`, `tool_end`, `turn_end`, `credit_warning`, `billing_blocked`, `error`, `pong`, `compaction` |

## Scheduled Jobs

<!-- provenance: CODE-FACTUAL -->
<!-- evidence: file=src/index.ts:642-701 -->

| Job | ID | Interval | Purpose |
|-----|----|----------|---------|
| R2 Sync | `r2_sync` | `config.syncIntervalMs` | Upload WAL segments to R2 |
| Git Sync | `git_sync` | `config.gitSyncIntervalMs` | Snapshot + push to git |
| Health | `health` | `config.healthIntervalMs` | System health aggregation |
| WAL Prune | `wal_prune` | 1 hour | Compact confirmed segments |

## External Service Dependencies

<!-- provenance: CODE-FACTUAL -->

| Service | Protocol | Auth | Failure Mode |
|---------|----------|------|--------------|
| Arrakis Billing | HTTPS + S2S JWT | ES256/HS256 | DLQ + 5min replay (`src/hounfour/billing-finalize-client.ts:123`) |
| Cheval Sidecar | Subprocess + temp file | HMAC-SHA256 | Fallback to native (`src/hounfour/cheval-invoker.ts:74`) |
| GitHub API | HTTPS | Bearer token | Warning, empty feed (`src/dashboard/activity-feed.ts:168`) |
| Redis | RESP3 (ioredis) | Connection string | In-memory fallback (`src/index.ts:174`) |
| PostgreSQL | TCP + TLS (Drizzle) | Connection string | No persistence (`src/index.ts:75`) |
| R2 / S3 | HTTPS + AWS SDK | AWS creds | Warning, no backup (`src/persistence/r2-sync.ts`) |
